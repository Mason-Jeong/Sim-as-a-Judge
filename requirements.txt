# Sim-as-a-Judge Python dependencies
# Isaac Sim packages (isaacsim, omni.*) are managed separately via conda/pip.
# Install these in the same conda environment where Isaac Sim is installed:
#   conda activate env_isaaclab
#   pip install -r requirements.txt

numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
datasets>=2.18.0
opencv-python-headless>=4.8.0
PyYAML>=6.0
